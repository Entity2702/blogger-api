version: '3.8'

services:
  database:
    image: mongo:latest
    container_name: database
    restart: always
    ports:
     - '27017:27017'
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password
    volumes:
      - mongo:/data/db

volumes:
  mongo: